connection:
  host: db.example.internal
  port: 3306
  user: service_user
  password: AnotherS3cr3t!
  database: service_db
pool:
  max_connections: 15
  min_idle: 1
  timeout_ms: 20000
replication:
  read_only: false
  replica_set:
  - db-replica-1.internal
  - db-replica-2.internal
