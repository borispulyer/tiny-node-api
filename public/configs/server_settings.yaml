server:
  instance: auth-service
  host: 0.0.0.0
  port: 9090
  threads: 8
cors:
  enabled: true
  allowed_origins:
  - https://auth.example.com
  - https://admin.example.com
rate_limit:
  enabled: true
  requests_per_minute: 120
